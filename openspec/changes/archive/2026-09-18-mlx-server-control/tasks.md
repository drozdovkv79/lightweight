## 1. ServerManager

- [x] 1.1 Создать `ServerManager` (`ObservableObject`): `server` пикер rapid-mlx/mlx_lm с персистом, `isRunning`, `log` с капом 200KB — проверить собирается и публикует состояния
- [x] 1.2 Реализовать `start(model:)`: pre-flight бинарника, `Process` через `/usr/bin/env`, stdout+stderr в лог, запись `provider_url` = 8008 — проверить процесс стартует, лог течёт, URL переключён
- [x] 1.3 Реализовать `stop()`: `terminate()` + SIGKILL fallback + sweep порта 8008, запись в лог — проверить процесс мёртв, память освобождена
- [x] 1.4 Останов при выходе из аппа (без сиротских процессов) — проверить после quit процесса сервера нет

## 2. Toolbar UI

- [x] 2.1 Тулбар: пикер сервера справа от меню модели, кнопка старт/стоп одной иконкой с состоянием — проверить иконка отражает `isRunning`, повторный старт не плодит процессы
- [x] 2.2 Кнопка лога рядом с настройками + отдельное окно лога (mono, автоскролл, лог переживает закрытие) — проверить вывод сервера виден, скролл внизу

## 3. Verification

- [ ] 3.1 Ручная проверка: старт/стоп обоих серверов, missing binary, занятый порт, чат после авто-switch, quit без сирот — проверить всё по сценариям `specs/mlx-server-control/spec.md` и `specs/provider-config/spec.md`
