import AppKit
import SwiftUI

enum OneDarkPro {
    static let background = Color(red: 0x28 / 255.0, green: 0x2C / 255.0, blue: 0x34 / 255.0)
    static let text = Color(red: 0xAB / 255.0, green: 0xB2 / 255.0, blue: 0xBF / 255.0)
    static let accent = Color(red: 0xC6 / 255.0, green: 0x78 / 255.0, blue: 0xDD / 255.0)
    static let string = Color(red: 0x98 / 255.0, green: 0xC3 / 255.0, blue: 0x79 / 255.0)

    static let nsBackground = NSColor(red: 0x28 / 255.0, green: 0x2C / 255.0, blue: 0x34 / 255.0, alpha: 1)
    static let nsText = NSColor(red: 0xAB / 255.0, green: 0xB2 / 255.0, blue: 0xBF / 255.0, alpha: 1)
}
